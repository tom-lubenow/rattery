[package]
name = "counter-app"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "The counter example app; build with --target wasm32-wasip2"

[lib]
crate-type = ["cdylib"]

[dependencies]
counter-shared = { path = "../shared" }
futures = { workspace = true }
rattery-app = { workspace = true }
