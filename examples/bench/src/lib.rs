//! Shared with the native baseline binary: the benchmark workloads, so the
//! same widgets and the same synthetic input run natively and inside the
//! component.

pub mod hover;
pub mod workloads;
