[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Developer tasks: `cargo xtask dev` runs the rebuild-and-reload loop"

[dependencies]
anyhow = { workspace = true }
clap = { version = "4", features = ["derive"] }
